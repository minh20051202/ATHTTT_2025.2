# Redundant Code Cleanup Audit

> Audit date: 2026-05-11
> Generated by: Automated codebase analysis

## Structure

- **IMMEDIATE** — safe to delete, zero side effects
- **CAUTION** — tests depend on it or it may be a planned future feature
- **DISCUSS** — requires design decision

---

## IMMEDIATE — Dead Frontend Components

These React components are defined but **never imported anywhere**. No other file references them.

| # | Component | File | Reason to Remove |
|---|-----------|------|-----------------|
| 1 | `BenchmarkCard` | `frontend/src/components/BenchmarkCard.jsx` | Never imported. `MetricsPanel.jsx` provides the same functionality and IS in use. Keeping both creates confusion about which is canonical. |
| 2 | `IntentCard` | `frontend/src/components/IntentCard.jsx` | Never imported. Intent display is handled inline in `ChatThread.jsx` via `AgentBubble`. |
| 3 | `TimingBreakdown` | `frontend/src/components/TimingBreakdown.jsx` | Never imported. Timing data shown inline in `ActionLog.jsx` and aggregated in `MetricsPanel.jsx`. |
| 4 | `AuthInfoPanel` | `frontend/src/components/AuthInfoPanel.jsx` | Never imported. Its role is covered by `ActionLog.jsx`. |
| 5 | `ProofAccordion` | `frontend/src/components/ProofAccordion.jsx` | Never imported. Proof details shown in `ActionLog.jsx` expandable steps. |
| 6 | `GhostResults` | `frontend/src/components/GhostResults.jsx` | Never imported. Empty-state message handled inline in `ChatThread.jsx`. |
| 7 | `ZKPFlow` | `frontend/src/components/ZKPFlow.jsx` | Marked `@deprecated` AND never imported. ZKP flow details now in `ActionLog.jsx`. |

**If deleted:** Delete all 7 files. Zero imports to fix.

---

## IMMEDIATE — Unused Frontend Service

| # | Module | File | Reason to Remove |
|---|--------|------|-----------------|
| 8 | `attacksApi` | `frontend/src/services/attacksApi.js` | Never imported. The `AttackPanel` in `Chat.jsx` is a pure frontend animation — it never calls the backend attack endpoints. The service module wraps `/api/attacks/*` calls but nothing uses it. |

**If deleted:** Delete `frontend/src/services/attacksApi.js`. Zero imports to fix.

---

## IMMEDIATE — Deprecated Backend Functions (always throw)

These functions exist solely to raise `NotImplementedError`. They cannot be called without crashing.

| # | Function | File | Reason to Remove |
|---|----------|------|-----------------|
| 9 | `hash_secret()` | `backend/auth/zkp.py:54-63` | Only raises `NotImplementedError`. Labeled "DEPRECATED" in docstring. Zero callers outside own definition. |
| 10 | `create_public_key()` | `backend/auth/zkp.py:66-75` | Only raises `NotImplementedError`. Labeled "DEPRECATED". The only caller was `create_client_signature()` which also dead-ends here. |
| 11 | `_ZKPAuth.create_client_signature()` | `backend/auth/zkp.py:206-214` | Calls `create_public_key()` which always raises. No callers. |

**If deleted:** Remove the 3 functions from `zkp.py`. Zero callers.

---

## IMMEDIATE — Dead Backend Code (never called)

These functions are defined but have zero callers anywhere in the codebase.

| # | Function | File | Reason to Remove |
|---|----------|------|-----------------|
| 12 | `_get_bcrypt()` | `backend/auth/oauth2.py:13-16` | Defined but never called. Lazy init pattern with no consumers. |
| 13 | `hash_password()` | `backend/auth/oauth2.py:27-29` | Never called. Password hashing for users is done inline in `DatabaseOperations.create_user()`. |
| 14 | `verify_password()` | `backend/auth/oauth2.py:31-33` | Never called. No password verification flow uses this. |
| 15 | `get_assertion_payload()` | `backend/auth/oauth2.py:112-121` | Never called. Decodes assertion payload without verification for inspection — but nothing inspects it. |
| 16 | `create_private_key_jwt()` | `backend/auth/oauth2.py:123-143` | Never called. Duplicates `create_client_assertion()` (line 83) which IS the active method. |
| 17 | `create_access_token_with_key()` | `backend/auth/oauth2.py:161-185` | Never called. Access tokens now use HS256 symmetric signing. This RS256 variant is dead. |
| 18 | `verify_token_with_public_key()` | `backend/auth/oauth2.py:187-210` | Never called. Token verification uses symmetric `verify_token()` instead. |
| 19 | `create_error_response()` | `backend/utils/errors.py:81-88` | Never imported or called. Error responses go through `AppError` (HTTPException subclass). |
| 20 | `_EXTERNAL_DHP` / `_DHQ` / `_DHG` | `backend/auth/zkp.py:36-38` | Set to the same values as `_DHP`/`_DHQ`/`_DHG` but never referenced. Leftover from an unimplemented "external import" pattern. |

**If deleted:** Remove the 9 dead items. Zero callers.

---

## IMMEDIATE — Unused CSS

These CSS rules and variables are defined but never referenced by any JSX class or inline style.

| # | CSS | File | Reason to Remove |
|---|-----|------|-----------------|
| 21 | `@keyframes shake` | `frontend/src/index.css:235-239` | No CSS class or inline style references it. All inline animations use unique local keyframes. |
| 22 | `@keyframes debounce-pulse` | `frontend/src/index.css:245-248` | Unreferenced. |
| 23 | `@keyframes hero-enter` | `frontend/src/index.css:252-261` | Comment says "Landing entrance" but there is no Landing page. The only route is Chat. |
| 24 | `.terminal-window` + variants (7 classes) | `frontend/src/index.css:145-343` | Full terminal-window component system. No JSX uses `className="terminal-window"`. |
| 25 | `@keyframes terminal-enter`, `line-enter` | `frontend/src/index.css:264-285` | Only used by dead `.terminal-window` classes. |
| 26 | `.stagger-children`, `@keyframes stagger-in` | `frontend/src/index.css:220-232` | Unreferenced. |
| 27 | `--duration-xs` through `--duration-lg` | `frontend/src/index.css:201-205` | All inline styles use hardcoded values like `120ms`, never `var(--duration-xs)`. |

**If deleted:** Remove ~150 lines from `index.css`. Verify no regressions by checking JSX className references.

---

## IMMEDIATE — Unused Imports

| # | Import | File | Reason to Remove |
|---|--------|------|-----------------|
| 28 | `field_validator`, `model_validator` | `backend/utils/config.py:2` | Neither used in `config.py`. |
| 29 | `HTTPException` | `backend/main.py:1` | `AppError` (HTTPException subclass) is used instead. |
| 30 | `intent_extractor`, `tool_caller` | `backend/main.py:13` | Only used in `api/chat.py`, not in `main.py`. |

**If deleted:** Remove 3 unused imports. Zero effect on runtime.

---

## CAUTION — Depends on Tests

These are referenced by tests. Removing them requires updating tests.

| # | Code | File | Test Files That Depend on It | Reason to Remove |
|---|------|------|------------------------------|-----------------|
| 31 | `_token_store` | `backend/auth/zkp.py:82` (dict) | `test_zkp.py` lines 11, 13, 21-27 | Duplicate of `_challenge_store` in `api/chat.py`. The API uses `chat.py`'s store; `zkp.py`'s store is vestigial. |
| 32 | `generate_token()` | `backend/auth/zkp.py:95-104` | `test_zkp.py` lines 11, 72, 77 | Duplicate. The API generates tokens inline in `api/chat.py`. |
| 33 | `consume_token()` | `backend/auth/zkp.py:107-118` | `test_zkp.py` lines 66, 77-82 | Duplicate. Token consumption is handled inline in `api/chat.py`. |
| 34 | `cleanup_expired_tokens()` | `backend/auth/zkp.py:86-92` | `test_zkp.py` lines 11, 23 | Duplicate of `_cleanup_expired_challenges()` in `api/chat.py`. |
| 35 | `_ZKPAuth.create_token()` | `backend/auth/zkp.py:240-245` | `test_zkp.py` lines 52, 91 | The API endpoint generates tokens directly. This wrapper is unused by the API. |
| 36 | `_ZKPAuth.sign_data()` | `backend/auth/zkp.py:247-271` | `test_zkp.py`, `test_chat.py`, `test_chat_trace.py` (many references) | The plan wants to deprecate password-based signing. But: `sign_data` also accepts raw hex private keys (line 261), which is how tests could migrate. However, removing it breaks 10+ tests. |

**Action:** If removing, delete the `_token_store`-related code (items 31-34), and either:
- (a) Keep `sign_data()` but accept only hex private keys (remove password fallback at line 264)
- (b) Keep `create_token()` since tests depend on it

---

## CAUTION — Future Feature Hooks

These exist for planned features that are not yet connected. Remove only if you've decided to abandon the feature.

| # | Code | File | Reason to Keep |
|---|------|------|----------------|
| 37 | `ConnectionManager` + `/ws/dashboard` | `backend/main.py:19-39, 133-144` | Real-time dashboard streaming. `vite.config.js` has a WebSocket proxy configured for it. If a live dashboard is planned, keep it. |
| 38 | Attack simulation endpoints | `backend/attacks/simulations.py` | Tested by `test_attacks.py`. If a real attack simulation UI is planned, keep these. The current `AttackPanel` is a frontend-only animation. |
| 39 | `socket.io-client` npm dep | `frontend/package.json` | Needed if WebSocket dashboard connects via Socket.IO |
| 40 | `recharts` npm dep | `frontend/package.json` | Needed if chart visualizations switch from CSS-based to recharts |

---

## DISCUSS — Unused Python Dependencies

| # | Package | File | Reason to Remove |
|---|---------|------|-----------------|
| 41 | `passlib[bcrypt]` | `backend/pyproject.toml` | No file imports `passlib`. The code uses `bcrypt` directly via `python-jose[cryptography]` transitives. Remove unless another feature needs it. |
| 42 | `aiosqlite` | `backend/pyproject.toml` | No file imports `aiosqlite`. SQLAlchemy runs synchronously. Remove unless async DB access is planned. |
| 43 | `python-dotenv` | `backend/pyproject.toml` | No file imports `dotenv`. `pydantic-settings` handles `.env` loading natively. Remove as unnecessary. |

---

## Summary by Risk

| Risk Level | Count | Items |
|------------|-------|-------|
| **IMMEDIATE** (safe delete) | 30 items | Dead components (1-7), unused service (8), deprecated stubs (9-11), dead functions (12-20), unused CSS (21-27), unused imports (28-30) |
| **CAUTION** (tests or future) | 10 items | Token store (31-34), test wrappers (35-36), WebSocket (37), attack endpoints (38), future deps (39-40) |
| **DISCUSS** (design call) | 3 items | Unused Python deps (41-43) |

**Total: 43 findings**

---

## Suggested Cleanup Order

1. Delete 7 dead components (items 1-7)
2. Delete unused service module (item 8)
3. Remove 3 deprecated stub functions from `zkp.py` (items 9-11)
4. Remove 7 dead functions from `oauth2.py` (items 12-18)
5. Remove `create_error_response()` from `errors.py` (item 19)
6. Remove `_EXTERNAL_*` vars from `zkp.py` (item 20)
7. Clean unused CSS from `index.css` (items 21-27)
8. Clean unused imports (items 28-30)
9. Discuss items 31-43 separately